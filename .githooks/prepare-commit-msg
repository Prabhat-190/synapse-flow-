#!/bin/sh
# Strip Cursor agent trailers so they do not appear on GitHub contributions.
grep -v '^Co-authored-by: Cursor <cursoragent@cursor.com>$' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
grep -v '^Made-with: Cursor$' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
